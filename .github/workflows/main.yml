name: Prueba de Caja de Herramientas TV3s

on:
  push:         # 👈 Agregamos esto solo para forzar el primer registro
  workflow_dispatch:

jobs:
  test-environment:
    runs-on: ubuntu-latest
    steps:
    - name: Clonar codigo
      uses: actions/checkout@v4

    - name: Saludo desde la nube
      run: echo "Desbloqueando los botones de la web..."
